{
  "name": "reward-calendar",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cd ..\\client && npm run build",
    "deploy": "npm run build && npm i && sls deploy",
    "deploy:production": "npm run build && npm i && sls deploy --stage production"
  },
  "devDependencies": {
    "fullstack-serverless": "^0.8.0"
  },
  "dependencies": {
    "google-auth-library": "^9.14.1",
    "google-spreadsheet": "^4.1.4"
  }
}
